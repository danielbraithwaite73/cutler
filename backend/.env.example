# Server
PORT=3000

# PostgreSQL (database: dinocamp)
# Option 1: connection URL
# DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/dinocamp

# Option 2: separate vars (used if DATABASE_URL is not set)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=dinocamp
